[project]
name = "animal-clash-atlas"
version = "0.1.0"
description = "Animal Clash Atlas MVP built with Streamlit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.44,<2",
]

[tool.uv]
package = false
